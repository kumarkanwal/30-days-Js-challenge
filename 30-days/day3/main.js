/* Math =  bilt in object that provides a collection of properties and methods  */

let x = 3;
let y = 2;
let z;

z = Math.round(x); // round to the nearest integer
z = Math.floor(x); // round to down
z = Math.ceil(x); // round to upper value
z = Math.trunc(x); // just removes decimal value
z = Math.pow(z, y);  // power on the number
z = Math.sqrt(x); // square root function
z = Math.log(x); // maths log value
z = Math.sin(x); // math tignomantory sin function
z = Math.cos(x); // math tignomantory cos function
z = Math.tan(x); // math tignomantory tan function
z = Math.abs(x); // eliminat the negative sign
z = Math.sign(z); // - value = -1 , 0 value = 0 , positive value = 1
let max = Math.max(x,y,z); // finds the maximum value
let min = Math.min(x,y,z); // finds the minimum value


